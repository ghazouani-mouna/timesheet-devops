#!/usr/bin/env groovy

pipeline {

    agent any
    tools{
    	maven "M3"
    }
    stages {
    	stage('Build Stage') { 
    		steps {
    			bat "ls"
	      		bat "mvn clean install"
	    	}
	 	}
	 	
    }

}