#!/usr/bin/env groovy

pipeline {

    agent any
    tools{
    	maven "M3"
    }
    stages {
    	stage('Build Stage') { 
    		steps {
    			sh "cd Timesheet-spring-boot-core-data-jpa-mvc-REST-1"
	      		sh "mvn clean install"
	    	}
	 	}
	 	
    }

}