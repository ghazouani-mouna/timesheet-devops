#!/usr/bin/env groovy

pipeline {

    agent any
    tools{
    	maven 'Maven 3.3.9'
    }

    stages {
    	stage('Build Stage') { 
    		steps {
	      		sh 'mvn clean insatll'
	    	}
	 	}
	 	
    }

}